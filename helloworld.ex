defmodule M do
  def main do
    IO.puts "Hello, world"
  end
end
#in this program we are goind to initialize a module HelloWorld
#then we have created a main function
#IO.puts (like print cmd) prints "Hello, world" .
